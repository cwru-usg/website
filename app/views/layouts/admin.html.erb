<!DOCTYPE html>
<html>
<head>
  <title>USG</title>
  <%= stylesheet_link_tag "admin", :media => "all" %>
  <link href='http://fonts.googleapis.com/css?family=Habibi|Open+Sans:400,300,700' rel='stylesheet' type='text/css'>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <script>
    //<![CDATA[
      $(function(){
        $("body").noisy({
          opacity: 0.07
        });
      });
    //]]>
  </script>
</head>
<body>
  <%= render "layout_partials/header" %>
  <div id="bar">
    <div id="container" class="logo">
      <div id="logo">
        <%= image_tag "logo.png" %>
        <h1><a href="#">Undergraduate Student Government</a>
      </div>
    </div>
    <% if !flash.empty? %>
      <div id="container">
      <% flash.each do |name, msg| %>
        <%= content_tag :div, raw(msg), :id => "flash", :class => "#{name}" if msg.is_a?(String) %>
      <% end %>
      </div>
    <% end %>
    <div id="container">
      <div id="admin_navigation">
        <ul>
          <li><%= link_to "Home", "/admin" %></li>
          <li><%= link_to "Users", admin_users_url %></li>
          <li><%= link_to "Pages", admin_pages_url %></li>
          <li><a href="#">Homepage</a></li>
        </ul>
      </div>
    </div>
  </div>
  <div id="container">
    <% if content_for?(:right) %>
      <div id="left">
        <%= yield %>
      </div>
      <div id="right">
        <%= yield(:right) %>
      </div>
    <% else %>
      <%= yield %>
    <% end %>
  </div>
  <footer id="footer">
    <div id="container">
      <p>The <strong>USG</strong> is a member of the <a href="#">Student Executive Council</a> at <a href="http://www.case.edu">Case Western Reserve University</a></p>
    </div>
  </footer>
</body>
</html>
